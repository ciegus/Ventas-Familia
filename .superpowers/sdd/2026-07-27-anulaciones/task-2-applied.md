# Task 2: anular_venta — Applied

**Migration Name:** `anular_venta`

**Summary:** SQL function `anular_venta(p_venta_id uuid, p_usuario_id uuid) RETURNS TABLE(folio text)` implemented and verified across all scenarios: contado stock restore, crédito balance math, insufficient-balance blocking, permission enforcement, and full cleanup. All tests passed.
